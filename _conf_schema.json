{
    "broadcast_max_delay": {
        "description": "广播消息的最大延迟秒数",
        "hint": "群发消息有风险，插件内采用随机延迟策略，降低风控的风险。此处设置随机延迟的最大值，单位为秒",
        "type": "float",
        "slider": {
            "min": 0.01,
            "max": 3.0,
            "step": 0.01
        },
        "default": 1.1
    },
    "skip_source": {
        "description": "是否跳过广播源头",
        "type": "bool",
        "hint": "发送广播命令时，是否跳过当前群聊/私聊，避免在源头会话重复发送广播消息",
        "default": true
    },
    "enable_gids": {
        "description": "开启广播的群聊",
        "type": "list",
        "hint": "只有此列表内的群聊才会收到广播消息",
        "default": []
    },
    "enable_uids": {
        "description": "开启私聊广播的好友",
        "type": "list",
        "hint": "只有此列表内的好友才会收到私聊广播",
        "default": []
    }
}
